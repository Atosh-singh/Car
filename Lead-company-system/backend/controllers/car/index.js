const {createCar} = require('./createCar');
const {deleteCar} = require('./deleteCar');
const {getCars} = require('./getCar');
const {updateCar} = require('./updateCar');

module.exports = {
    createCar,
    deleteCar,
    getCars,
    updateCar
}